services:
  mailpit:
    image: axllent/mailpit:latest
    container_name: ultima-mailpit-app
    restart: unless-stopped
    ports:
      - "1025:1025"
      - "8025:8025"

  app:
    image: node:22
    container_name: ultima-app
    working_dir: /app
    volumes:
      - ./:/app
    command: sh -c "npm install && npm run server:localdb:dev"
    environment:
      DB_CLIENT: postgres
      PG_HOST: postgres
      PG_PORT: 5432
      PG_USER: postgres
      PG_PASSWORD: admin
      PG_DATABASE: ultima_demo
      PORT: 3001
      PUBLIC_WEB_BASE_URL: http://localhost:5173
      JWT_SECRET: ultima-demo-secret
      SMTP_HOST: mailpit
      SMTP_PORT: 1025
      SMTP_SECURE: "false"
      SMTP_FROM: noreply@ultima.local
    ports:
      - "3001:3001"
    depends_on:
      postgres:
        condition: service_healthy
      mailpit:
        condition: service_started
